// [XJC] 通用能力对齐 · 编译期功能开关（任务书 §3.2）
// 本地能力（语音/知识库）离线必须可用，因此用编译期常量门控，
// 禁止用远程配置作为唯一开关（离线版拿不到远程配置）。
// 各任务卡（T-A1/T-A2）实现完成并自测通过后，由对应子代理把开关翻为 true。

/** 语音输入/播报（T-A2）：麦克风按钮、朗读按钮、设置页「语音」Tab */
export const VOICE_ENABLED = true

/** 知识库 RAG（T-A1）：侧边栏「知识库」入口与页面 */
export const KNOWLEDGE_ENABLED = true
